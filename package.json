{
  "name": "jwt-sso-auth",
  "version": "1.0.0",
  "description": "JWT-based SSO authentication system",
  "main": "index.js",
  "scripts": {
    "start:sso": "cd sso-server && npm start",
    "start:client1": "cd client1 && npm start",
    "start:client2": "cd client2 && npm start",
    "install:all": "npm install && cd sso-server && npm install && cd ../client1 && npm install && cd ../client2 && npm install",
    "dev": "concurrently \"npm run start:sso\" \"npm run start:client1\" \"npm run start:client2\""
  },
  "keywords": [
    "jwt",
    "sso",
    "authentication",
    "react"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "jwt-sso-auth": "file:"
  }
}
